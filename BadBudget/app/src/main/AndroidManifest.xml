<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".BadBudgetApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BadBudget"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.BadBudget">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.BadBudget" />

        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.BadBudget" />

        <activity
            android:name=".AddExpenseActivity"
            android:exported="false"
            android:theme="@style/Theme.BadBudget" />

        <activity
            android:name=".BudgetsActivity"
            android:exported="false"
            android:theme="@style/Theme.BadBudget" />

        <activity android:name=".SpendSummaryActivity"
            android:exported="false"/>

        <activity android:name=".AwardsActivity"
            android:exported="false"/>

        <activity android:name=".ExpenseHistoryActivity"
            android:exported="false"/>

        <activity android:name=".ReceiptViewerActivity"
            android:exported="false"/>

        <activity android:name=".InsightsActivity"
            android:exported="false"/>

    </application>
</manifest>
